---
name: check
description: verify changes build and pass
---

run your build commands and verify everything passes. run tests if they exist. figure out what to check from repo context (package.json, Makefile, etc.) or ask if unclear.
